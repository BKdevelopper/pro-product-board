
export interface Product {
  id: number; // Auto-incrementing ID (internal use only)
  serialID: string; // Serial number displayed to users
  url1: string; // Vinted
  url2: string; // La Chiffo
  url3: string; // LabelEmmaus
  emplacement: string;
}
